// lib/content/data/spirits.ts
// Spirits don't have mock data yet — placeholder for admin editor data

import type { SpiritItem } from "../types";

const items: SpiritItem[] = [];
export default items;
export { items };
