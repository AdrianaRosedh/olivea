// lib/content/data/promotions.ts
// Promotions — placeholder until admin editor populates

import type { Promotion } from "../types";

const items: Promotion[] = [];
export default items;
export { items };
