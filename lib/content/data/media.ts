// lib/content/data/media.ts
// Media library — placeholder until Supabase storage is connected

import type { MediaItem } from "../types";

const items: MediaItem[] = [];
export default items;
export { items };
