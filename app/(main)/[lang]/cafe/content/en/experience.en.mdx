import StickyBlock from '@/components/scroll/StickyBlock'
import Reveal from '@/components/scroll/Reveal'
import Sub from '@/components/scroll/Sub'
import ParallaxImage from '@/components/scroll/ParallaxImage'

<StickyBlock
  id="experience"
  className="main-section snap-center"
  title={null}
  containerClassName="w-full md:max-w-[min(900px,92vw)] md:mx-auto"
>
  <Reveal preset="up"><h2 className="text-2xl md:text-4xl font-semibold mb-4 md:mb-6">Experience</h2></Reveal>

  <div className="space-y-6 md:space-y-8">
    <Sub id="origin" preset="blur-up">Our coffee begins with many hands…</Sub>
    <Sub id="bean" preset="up">We select traceable lots…</Sub>

    <div className="relative overflow-hidden rounded-[28px] ring-1 ring-black/10 shadow-[0_20px_60px_-20px_rgba(0,0,0,0.35)]">
      <ParallaxImage src="/images/cafe/roast.jpg" alt="Precise roast to elevate origin" heightVh={46} speed={0.22} />
    </div>

    <Sub id="roast" preset="left">Roasting is precise and restrained…</Sub>
    <Sub id="water" preset="right">Water is part of the recipe…</Sub>
    <Sub id="bar" preset="fade">At the bar we serve coffee with memory…</Sub>
  </div>
</StickyBlock>