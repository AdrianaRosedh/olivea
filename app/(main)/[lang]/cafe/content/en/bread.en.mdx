import StickyBlock from '@/components/scroll/StickyBlock'
import Reveal from '@/components/scroll/Reveal'
import Sub from '@/components/scroll/Sub'
import ParallaxImage from '@/components/scroll/ParallaxImage'

<StickyBlock
  id="bread"
  className="main-section snap-center"
  title={null}
  containerClassName="w-full md:max-w-[min(900px,92vw)] md:mx-auto"
>
  <Reveal preset="up"><h2 className="text-2xl md:text-4xl font-semibold mb-4 md:mb-6">Bread</h2></Reveal>

  <div className="space-y-6 md:space-y-8">
    <Sub id="flours" preset="up">A lifelong love for bread…</Sub>
    <Sub id="recipes" preset="left">Our own recipes in dialogue with regional bakers…</Sub>

    <div className="relative overflow-hidden rounded-[28px] ring-1 ring-black/10 shadow-[0_20px_60px_-20px_rgba(0,0,0,0.35)]">
      <ParallaxImage src="/images/cafe/pan.jpg" alt="House bread — crust, crumb and aroma" heightVh={46} speed={0.22} />
    </div>

    <Sub id="heritage" preset="right">Every flour we use has two roots…</Sub>
    <Sub id="ritual" preset="fade">Bread completes your coffee…</Sub>
  </div>
</StickyBlock>