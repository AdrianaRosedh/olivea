import { Section } from "@/components/sections/Section"

export default function CulinaryPhilosophy({ dict }) {
  const sec = dict.farmtotable.sections.culinary_philosophy

  return (
    <Section id="culinary_philosophy" className="main-section">
      <h2 id="culinary_philosophy-heading">{sec.title}</h2>
      <p>{sec.description}</p>

      {sec.subsections &&
        Object.entries(sec.subsections).map(([subId, sub]) => (
          <Section key={subId} id={subId} className="subsection">
            <h3 id={`${subId}-heading`}>{sub.title}</h3>
            <p>{sub.description}</p>
          </Section>
        ))}
    </Section>
  )
}