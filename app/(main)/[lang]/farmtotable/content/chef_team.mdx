import { Section } from "@/components/sections/Section"

export default function ChefTeam({ dict }) {
  const sec = dict.farmtotable.sections.chef_team

  return (
    <Section id="chef_team" className="main-section">
      <h2 id="chef_team-heading">{sec.title}</h2>
      <p>{sec.description}</p>

      {sec.subsections &&
        Object.entries(sec.subsections).map(([subId, sub]) => (
          <Section key={subId} id={subId} className="subsection">
            <h3 id={`${subId}-heading`}>{sub.title}</h3>
            <p>{sub.description}</p>
          </Section>
        ))}
    </Section>
  )
}