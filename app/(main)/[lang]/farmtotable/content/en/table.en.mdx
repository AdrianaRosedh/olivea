import StickyBlock from "@/components/scroll/StickyBlock";
import Reveal from "@/components/scroll/Reveal";
import Sub from "@/components/scroll/Sub";
import ParallaxImage from "@/components/scroll/ParallaxImage";
import FieldNote from "@/components/mdx/FieldNote";

<StickyBlock
  id="table"
  variant="top"
  containerClassName="w-full md:max-w-[min(900px,92vw)] md:mx-auto"
  className="py-12 md:py-20"
  title={
    <Reveal preset="up">
      <h2 className="text-2xl md:text-4xl font-semibold">
        The table
      </h2>
    </Reveal>
  }
>
  {/* BLOCK 1 — text aligned with the title */}
  <div className="max-w-[720px] w-full text-left">
    <Sub id="hospitality" preset="up">
      <p>
        We host friends. Service is present, discreet, and attentive to the
        rhythm of each table.
      </p>
      <p className="mt-3">
        We read culture, allergies, and preferences. Timing and temperature
        are adjusted so each dish arrives at its ideal point.
      </p>
    </Sub>
  </div>

  {/* PARALLAX IMAGE (central card) */}
  <div
    className="
      relative w-full max-w-[950px] mx-auto my-10
      overflow-hidden rounded-[28px]
      ring-1 ring-black/10
      shadow-[0_20px_60px_-20px_rgba(0,0,0,0.35)]
    "
  >
    <ParallaxImage
      src="/images/farm/dining.jpg"
      alt="Rhythm and hospitality"
      heightVh={56}
      speed={0.2}
      className="rounded-[28px]"
      fit="cover"
      objectPosition="50% 50%"
    />
  </div>

  {/* BLOCK 2 — paragraph + centered box */}
  <div className="max-w-[720px] w-full text-left">
    <Sub
      id="narrative"
      preset="fade"
      className="!min-h-0"
    >
      Each course adds context: its origin, the hands that care for it, and
      the reasoning behind each pairing. An honest narrative, without artifice.
    </Sub>
  </div>

  {/* CENTERED NOTE, SAME WIDTH AS IMAGE */}
  <div className="w-full max-w-[950px] mx-auto mt-4 flex justify-center">
    <FieldNote className="max-w-[720px] w-full">
      We offer a <strong>garden-view terrace</strong> and warm interior spaces.
      Tables are assigned according to your preferences and service flow.
      We are <strong>pet friendly</strong> in designated terrace areas and can
      also assist in coordinating
      <strong> celebrations or special events</strong> with prior notice.
    </FieldNote>
  </div>

  {/* PLATES GALLERY */}
  <div className="w-full max-w-[950px] mx-auto pt-8 grid gap-4 md:grid-cols-2 md:gap-6">
    <div className="relative overflow-hidden rounded-2xl ring-1 ring-black/10 shadow-[0_20px_60px_-24px_rgba(0,0,0,0.45)]">
      <ParallaxImage
        src="/images/farm/plates-1.jpg"
        alt="Correct temperature"
        heightVh={45}
        speed={0.12}
        className="h-full"
      />
    </div>

    <div className="relative overflow-hidden rounded-2xl ring-1 ring-black/10 shadow-[0_20px_60px_-24px_rgba(0,0,0,0.45)]">
      <ParallaxImage
        src="/images/farm/plates-2.jpg"
        alt="Course cadence"
        heightVh={45}
        speed={0.12}
        className="h-full"
      />
    </div>
  </div>
</StickyBlock>