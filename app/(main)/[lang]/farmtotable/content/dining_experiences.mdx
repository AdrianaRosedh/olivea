import { Section } from "@/components/sections/Section"

export default function DiningExperiences({ dict }) {
  const sec = dict.farmtotable.sections.dining_experiences

  return (
    <Section id="dining_experiences" className="main-section">
      <h2 id="dining_experiences-heading">{sec.title}</h2>
      <p>{sec.description}</p>

      {sec.subsections &&
        Object.entries(sec.subsections).map(([subId, sub]) => (
          <Section key={subId} id={subId} className="subsection">
            <h3 id={`${subId}-heading`}>{sub.title}</h3>
            <p>{sub.description}</p>
          </Section>
        ))}
    </Section>
  )
}