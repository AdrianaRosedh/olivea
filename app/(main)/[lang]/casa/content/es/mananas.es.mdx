"use client";

import StickyBlock from "@/components/scroll/StickyBlock";
import Reveal from "@/components/scroll/Reveal";
import { motion } from "framer-motion";
import Image from "next/image";

export default function Page() {
  return (
    <StickyBlock
      id="mananas"
      variant="top"
      className="pt-10 pb-6 md:pt-16 md:pb-12 snap-start"
      containerClassName="w-full md:max-w-[min(1080px,92vw)] md:mx-auto"
      title={null}
    >
      {/* Imagen principal */}
      <motion.div
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1 }}
        transition={{ duration: 0.8 }}
        viewport={{ once: true }}
        className="relative w-full h-[60vh] md:h-[70vh] rounded-[28px] overflow-hidden ring-1 ring-black/10 shadow-xl mb-6 md:mb-10"
      >
        <Image
          src="/images/casa/morning.jpg"
          alt="Mañanas en Casa Olivea"
          fill
          className="object-cover"
          quality={80}
          sizes="100vw"
          priority
        />
      </motion.div>

      {/* Título debajo de la imagen */}
      <Reveal preset="up">
        <h2 className="text-2xl md:text-4xl font-semibold mb-8 text-oliveaText">
          Mañanas en Olivea
        </h2>
      </Reveal>

      {/* Texto en columnas */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-10 text-base text-oliveaText leading-relaxed">
        {/* Desayuno */}
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5 }}
          viewport={{ once: true }}
          id="desayuno"
        >
          <p className="text-lg font-medium text-oliveaText/80 mb-2">
            Desayuno
          </p>

          <p className="text-oliveaText/80">
            Los huéspedes del hotel comienzan el día con un desayuno continental
            diseñado por la cocina de Olivea. Pan hecho en casa, vegetales
            de nuestro huerto y una selección de acompañamientos locales y preparados
            internamente — servido de forma simple, fresca y equilibrada.
          </p>

          <p className="mt-3 text-sm text-oliveaText/70">
            El café incluido en el desayuno es americano o té.
          </p>
        </motion.div>

        {/* Café & Olivea Café */}
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, delay: 0.1 }}
          viewport={{ once: true }}
          id="cafe"
        >
          <p className="text-lg font-medium text-oliveaText/80 mb-2">
            Café & Olivea Café
          </p>

          <p className="text-oliveaText/80">
            Para quienes buscan una mañana más amplia — o un desayuno más
            expresivo — Olivea Café ofrece un menú extendido y una selección
            completa de bebidas de café. Bebidas con espresso, preparaciones
            especiales y platos adicionales están disponibles directamente en el
            café.
          </p>
        </motion.div>
      </div>
    </StickyBlock>
  );
}