// app/(main)/[lang]/casa/content/es/diseno.es.mdx
"use client";

import StickyBlock from "@/components/scroll/StickyBlock";
import Reveal from "@/components/scroll/Reveal";
import { motion } from "framer-motion";
import Image from "next/image";
import ScrollDrift from "@/components/scroll/ScrollDrift";

export default function Page() {
  return (
    <StickyBlock
      id="diseno"
      variant="top"
      className="pt-10 pb-6 md:pt-16 md:pb-12 snap-start"
      containerClassName="w-full md:max-w-[min(1080px,92vw)] md:mx-auto"
      title={
        <div className="md:hidden">
          <Reveal preset="up">
            <h2 className="text-2xl md:text-4xl font-semibold">
              Diseño que se Siente
            </h2>
          </Reveal>

          <p className="mt-2 text-sm italic text-gray-500">
            Diseño concebido por Ange Joy, fundadora de Olivea.
          </p>
        </div>
      }
    >
      <div className="flex flex-col md:flex-row gap-10 items-center">
        {/* Imagen IZQUIERDA — ScrollDrift (reveal + drift) */}   
        <ScrollDrift
          fromX={-48}
          toX={22}
          reveal
          className="w-full md:w-1/2"
        >
          <div className="w-full h-[50vh] relative rounded-[28px] overflow-hidden ring-1 ring-black/10 shadow-xl">
            <Image
              src="/images/casa/InteriorRooms1.jpg"
              alt="Design textures: clay, cotton, light"
              fill
              className="object-cover"
              sizes="(max-width: 768px) 100vw, 500px"
              quality={80}
              priority
            />
          </div>
        </ScrollDrift>
        
        {/* Texto DERECHA */}
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.9, delay: 0.18 }}
          viewport={{ once: true }}
          className="w-full md:w-1/2 text-gray-800 space-y-5"
        >
          {/* Desktop title + byline */}
          <Reveal preset="up">
            <h2 className="hidden md:block text-3xl lg:text-4xl font-semibold mb-2">
              Diseño que se Siente
            </h2>
          </Reveal>

          <p className="hidden md:block text-sm italic text-gray-500 mb-4">
            Diseño concebido por Ange Joy, fundadora de Olivea.
          </p>

          <p className="text-lg" id="iluminacion">
            La luz guía nuestra arquitectura. Por la mañana es suave e indirecta.
            Por la tarde, cálida y envolvente. Cada superficie evita el
            deslumbramiento y favorece la calma.
          </p>

          <p className="text-base text-gray-700 leading-snug" id="materiales">
            Usamos piedra, madera, algodón y barro — materiales honestos que
            envejecen con belleza y transmiten calidez. Nada busca llamar la
            atención. Todo invita a tocar.
          </p>

          <p className="text-base text-gray-700 leading-snug" id="sostenibilidad">
            Agua en vidrio. Amenidades rellenables. Energía solar. La
            sostenibilidad está integrada — sin necesidad de anunciarla.
          </p>
        </motion.div>
      </div>
    </StickyBlock>
  );
}