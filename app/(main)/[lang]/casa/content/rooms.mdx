import { Section } from "@/components/sections/Section"

export default function Rooms({ dict }) {
  const sec = dict.casa.sections.rooms

  return (
    <Section id="rooms" className="main-section">
      <h2 id="rooms-heading">{sec.title}</h2>
      <p>{sec.description}</p>

      {sec.subsections &&
        Object.entries(sec.subsections).map(([subId, sub]) => (
          <Section key={subId} id={subId} className="subsection">
            <h3 id={`${subId}-heading`}>{sub.title}</h3>
            <p>{sub.description}</p>
          </Section>
        ))}
    </Section>
  )
} 