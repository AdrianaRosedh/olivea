import { Section } from "@/components/sections/Section"

export default function Dining({ dict }) {
  const sec = dict.casa.sections.dining

  return (
    <Section id="dining" className="main-section">
      <h2 id="dining-heading">{sec.title}</h2>
      <p>{sec.description}</p>

      {sec.subsections &&
        Object.entries(sec.subsections).map(([subId, sub]) => (
          <Section key={subId} id={subId} className="subsection">
            <h3 id={`${subId}-heading`}>{sub.title}</h3>
            <p>{sub.description}</p>
          </Section>
        ))}
    </Section>
  )
}