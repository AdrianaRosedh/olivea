'use client';

import StickyBlock from "@/components/scroll/StickyBlock";
import Reveal from "@/components/scroll/Reveal";
import { motion } from "framer-motion";
import Image from "next/image";

export default function Page() {
  return (
    <StickyBlock
      id="courtyard"
      variant="top"
      className="pt-10 pb-6 md:pt-16 md:pb-12 snap-start"
      containerClassName="w-full md:max-w-[min(1080px,92vw)] md:mx-auto"
      title={
        <Reveal preset="up">
          <h2 className="text-2xl md:text-4xl font-semibold">Spaces to Breathe</h2>
        </Reveal>
      }
    >
      <div className="flex flex-col md:flex-row gap-10 md:items-center">
        {/* Text first (always visible) */}
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6 }}
          viewport={{ once: true }}
          className="w-full md:w-1/2 text-gray-800 space-y-5"
        >
          <p className="text-lg">
            Inspired by old family homes, the courtyard opens at the heart of the property.
            Two long corridors connect your room to open air and rhythm.
          </p>
          <p className="text-base text-gray-700 leading-snug">
            This is not a showpiece. It’s a lived-in calm — gravel, aromatic plants,
            and a sunlit pool that frames the seasons. Light is filtered. Sound is soft.
          </p>
          <p className="text-base text-gray-700 leading-snug">
            The courtyard shelters you through the day — whether you step out or glance through the window.
          </p>
        </motion.div>

        {/* Image visible on all breakpoints */}
        <motion.div
          initial={{ opacity: 0, x: 60 }}
          whileInView={{ opacity: 1, x: 0 }}
          transition={{ duration: 1 }}
          viewport={{ once: true }}
          className="w-full md:w-1/2 h-[50vh] relative rounded-[28px] overflow-hidden ring-1 ring-black/10 shadow-xl"
        >
          <Image
            src="/images/casa/patio.jpg"
            alt="Courtyard framed by olive trees"
            fill
            className="object-cover"
            sizes="(max-width: 768px) 100vw, 50vw"
            quality={80}
            priority
          />
        </motion.div>
      </div>
    </StickyBlock>
  );
}
