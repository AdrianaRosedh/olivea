"use client";

import StickyBlock from "@/components/scroll/StickyBlock";
import Reveal from "@/components/scroll/Reveal";
import { motion } from "framer-motion";
import Image from "next/image";
import ScrollDrift from "@/components/scroll/ScrollDrift";

export default function Page() {
  return (
    <StickyBlock
      id="design"
      variant="top"
      className="pt-10 pb-6 md:pt-16 md:pb-12 snap-start"
      containerClassName="w-full md:max-w-[min(1080px,92vw)] md:mx-auto"
      title={
        <div className="md:hidden">
          <Reveal preset="up">
            <h2 className="text-2xl md:text-4xl font-semibold text-oliveaText">
              Design That Feels
            </h2>
          </Reveal>

          <p className="mt-2 text-sm italic text-oliveaText/70">
            Design conceived by Ange Joy, founder of Olivea.
          </p>
        </div>
      }
    >
      <div className="flex flex-col md:flex-row gap-10 items-center">
        <ScrollDrift fromX={-48} toX={22} reveal className="w-full md:w-1/2">
          <div className="w-full h-[50vh] relative rounded-[28px] overflow-hidden ring-1 ring-black/10 shadow-xl">
            <Image
              src="/images/casa/InteriorRooms1.jpg"
              alt="Design textures: clay, cotton, natural light"
              fill
              className="object-cover"
              sizes="(max-width: 768px) 100vw, 500px"
              quality={80}
              priority
            />
          </div>
        </ScrollDrift>

        <motion.div
          initial={{ opacity: 0, y: 30 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.9, delay: 0.18 }}
          viewport={{ once: true }}
          className="w-full md:w-1/2 text-oliveaText space-y-5"
        >
          <Reveal preset="up">
            <h2 className="hidden md:block text-3xl lg:text-4xl font-semibold mb-2 text-oliveaText">
              Design That Feels
            </h2>
          </Reveal>

          <p className="hidden md:block text-sm italic text-oliveaText/70 mb-4">
            Design conceived by Ange Joy, founder of Olivea.
          </p>

          <p className="text-lg text-oliveaText/80" id="iluminacion">
            Light leads the architecture. In the morning, it’s soft and indirect.
            By afternoon, warm and grounding. Every surface avoids glare,
            favoring calm.
          </p>

          <p className="text-base text-oliveaText/80 leading-snug" id="materiales">
            We use stone, wood, cotton, and clay — materials that age well and
            hold warmth. Nothing calls attention. Everything invites touch.
          </p>

          <p className="text-base text-oliveaText/80 leading-snug" id="sostenibilidad">
            Glass water. Refillable amenities. Solar power. Sustainability is
            embedded — not signposted.
          </p>
        </motion.div>
      </div>
    </StickyBlock>
  );
}