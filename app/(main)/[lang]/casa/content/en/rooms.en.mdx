'use client';

import StickyBlock from "@/components/scroll/StickyBlock";
import Reveal from "@/components/scroll/Reveal";
import { motion } from 'framer-motion';
import Image from "next/image";

export default function Page() {
  return (
    <StickyBlock
      id="rooms"
      variant="top"
      className="pt-10 pb-6 md:pt-16 md:pb-12 snap-start"
      containerClassName="w-full md:max-w-[min(1080px,92vw)] md:mx-auto"
      title={
        <Reveal preset="up">
          <h2 className="text-2xl md:text-4xl font-semibold">Our Suites</h2>
        </Reveal>
      }
    >
      <motion.div
        initial={{ opacity: 0, y: 50 }}
        whileInView={{ opacity: 1, y: 0 }}
        transition={{ duration: 1.2 }}
        viewport={{ once: true }}
        className="text-lg text-gray-800 leading-relaxed max-w-[720px] space-y-5"
      >
        <p>
          Fourteen suites — each named after an aromatic plant — offer space, warmth, and a sense of return.
          This is where your rhythm resets.
        </p>
        <p>
          King-size beds, blackout curtains, and natural fiber textures invite deep rest. All rooms are designed for two adults — quiet, light-filled, and considered.
        </p>
      </motion.div>

      <motion.div
        initial={{ opacity: 0, y: 40 }}
        whileInView={{ opacity: 1, y: 0 }}
        transition={{ duration: 1.2, delay: 0.2 }}
        viewport={{ once: true }}
        className="mt-10 relative w-full max-w-5xl h-[50vh] rounded-[28px] overflow-hidden ring-1 ring-black/10 shadow-xl"
      >
        <Image
          src="/images/casa/room.jpg"
          alt="Suite with soft textures and calm lighting"
          fill
          className="object-cover"
          sizes="(max-width: 768px) 100vw, 900px"
          quality={80}
          priority
        />
      </motion.div>

      <motion.div
        initial={{ opacity: 0, y: 30 }}
        whileInView={{ opacity: 1, y: 0 }}
        transition={{ duration: 1.1, delay: 0.4 }}
        viewport={{ once: true }}
        className="text-base text-gray-700 leading-snug max-w-[700px] mt-6"
        id="luz"
      >
        <p>
          Light shifts throughout the day. In the morning, it filters softly through curtains.
          In the evening, it settles — warm, indirect, grounding.
        </p>
      </motion.div>
    </StickyBlock>
  );
}
